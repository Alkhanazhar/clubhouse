/* eslint-disable react/prop-types */
const StepsAvatar = ({ onNext }) => {
  return (
    <div>
      StepsAvatar
      <button onClick={onNext}>next</button>
    </div>
  );
};

export default StepsAvatar;
